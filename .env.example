IMAGE_NAME=tsshadow/lms
TAG=alpha
SERVICE_NAME=lms-alpha

# Remote Deployment (optional)
# Descriptive name for the deployment target (e.g., "Production Stack")
DEPLOY_TARGET_NAME=

# Option 1: Portainer Webhook (Recommended if using Portainer Stacks)
PORTAINER_WEBHOOK_URL=

# Option 2: SSH-based deployment
REMOTE_HOST=
REMOTE_USER=
REMOTE_PASS=
# Optional: If STACK_DIR is set, the script will update the whole stack using docker-compose
STACK_DIR=
